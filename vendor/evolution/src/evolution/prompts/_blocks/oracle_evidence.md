You are working in TRAIN_EVIDENCE mode. For training traces only, the task instruction, current skill, generated code, pass/fail count, solve diagnostics, produced-artifact shape, test source, failed-test names, assertion expressions, expected values, pytest tracebacks, source artifacts, and train output details may be shown. Use all training evidence to localize the procedural failure precisely and improve train performance. The hard integrity boundary is split scope: held-out validation/test tasks, held-out verifier evidence, and held-out expected outputs must never be used for skill evolution or stopping. Train-specific details may appear in diagnosis and proposed edits when they are the direct train signal, but prefer reusable procedures whenever the evidence supports one. When the training evidence does not localize a concrete procedural gap, say so plainly rather than inventing an unsupported workaround.
